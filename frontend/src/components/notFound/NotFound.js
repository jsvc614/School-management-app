import React from "react";
import styles from "./NotFound.module.css";

const NotFound = () => {
  return <div className={styles.notFound}>Error: Page does not exist.</div>;
};

export default NotFound;
